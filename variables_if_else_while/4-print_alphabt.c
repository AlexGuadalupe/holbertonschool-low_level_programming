#include <stdio.h>
/**
*main - Print all the letters except q and e
*Return: Always 0
*/
int main(void)
{
char a
for (a = 'a'; a <= 'z'; c++)

if (a != 'e' && a != 'z')
putchar(c);
putchar('\n');
return (0);
}
